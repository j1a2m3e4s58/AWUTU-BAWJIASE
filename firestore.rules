rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    function signedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return signedIn() && request.auth.token.admin == true;
    }

    function isOwner(userId) {
      return signedIn() && request.auth.uid == userId;
    }

    function isPublished() {
      return resource.data.published == true;
    }

    // Temporary launch mode:
    // The current admin uses an in-app hidden password, not Firebase Auth claims.
    // These writes are open so admin content can save until real authentication is added.
    function launchAdminAccess() {
      return true;
    }

    match /users/{userId} {
      allow read: if isOwner(userId) || isAdmin();
      allow create: if isOwner(userId) || isAdmin();
      allow update: if isOwner(userId) || isAdmin();
      allow delete: if isAdmin();
    }

    match /announcements/{documentId} {
      allow read: if true;
      allow create, update, delete: if launchAdminAccess();
    }

    match /archiveDocuments/{documentId} {
      allow read: if true;
      allow create, update, delete: if launchAdminAccess();
    }

    match /communityEvents/{documentId} {
      allow read: if true;
      allow create, update, delete: if launchAdminAccess();
    }

    match /galleryItems/{documentId} {
      allow read: if true;
      allow create, update, delete: if launchAdminAccess();
    }

    match /historyContents/{documentId} {
      allow read: if true;
      allow create, update, delete: if launchAdminAccess();
    }

    match /kings/{documentId} {
      allow read: if true;
      allow create, update, delete: if launchAdminAccess();
    }

    match /trainingVideos/{documentId} {
      allow read: if true;
      allow create, update, delete: if launchAdminAccess();
    }

    match /comments/{documentId} {
      allow read: if true;
      allow create: if true;
      allow update, delete: if launchAdminAccess();
    }

    match /auditLogs/{documentId} {
      allow read, create, update, delete: if launchAdminAccess();
    }

    match /siteSettings/{documentId} {
      allow read: if true;
      allow create, update, delete: if launchAdminAccess();
    }

    match /tributeMessages/{documentId} {
      allow read: if true;
      allow create: if true;
      allow update, delete: if launchAdminAccess();
    }

    match /contactMessages/{documentId} {
      allow create: if true;
      allow read, update, delete: if launchAdminAccess();
    }
  }
}
